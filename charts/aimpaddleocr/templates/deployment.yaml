{{- $gpuType := include "aimpaddleocr.gpuType" . -}}
{{- $isBlackwell := include "aimpaddleocr.isBlackwell" . -}}
{{- $engineImage := include "aimpaddleocr.engineImage" . -}}
{{- $isNvidia := eq $gpuType "nvidia" -}}
{{- $gpuMemMi := "6144" -}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: '{{ .Release.Name }}'
  namespace: '{{ .Release.Namespace }}'
  labels:
    io.kompose.service: aimpaddleocr
  annotations:
    {{- if $isNvidia }}
    applications.app.bytetrade.io/gpu-inject: "aimpaddleocr"
    {{- end }}
    debug.aimpaddleocr/gpu-type: {{ $gpuType | quote }}
    debug.aimpaddleocr/is-blackwell: {{ $isBlackwell | quote }}
    debug.aimpaddleocr/engine-image: {{ $engineImage | quote }}
    debug.aimpaddleocr/nodes-len: {{ len (.Values.nodes | default (list)) | quote }}
    {{- with (first (.Values.nodes | default (list))) }}
    {{- $n := (toJson .) | fromJson }}
    debug.aimpaddleocr/node0-kind: {{ kindOf . | quote }}
    debug.aimpaddleocr/node0-json: {{ toJson . | quote }}
    debug.aimpaddleocr/node0-gpus-len: {{ len ((index $n "gpus") | default (list)) | quote }}
    {{- with (first ((index $n "gpus") | default (list))) }}
    debug.aimpaddleocr/gpu0-arch: {{ (index . "arch") | quote }}
    debug.aimpaddleocr/gpu0-vendor: {{ (index . "vendor") | quote }}
    debug.aimpaddleocr/gpu0-modelname: {{ (index . "modelName") | quote }}
    {{- end }}
    {{- end }}
spec:
  replicas: {{ .Values.workloads.aimpaddleocr.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      io.kompose.service: aimpaddleocr
  template:
    metadata:
      labels:
        io.kompose.network/chrome-default: "true"
        io.kompose.service: aimpaddleocr
    spec:
      restartPolicy: Always
      volumes:
        - name: pipeline-config
          configMap:
            name: pipeline-config
            defaultMode: 0444
        - name: nginx-config
          configMap:
            name: nginx-config
            defaultMode: 0444
      containers:
        - name: aimpaddleocr
          image: "{{ $engineImage }}"
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/paddlex
            - --serve
            - --pipeline
            - /home/paddleocr/pipeline_config_fastdeploy.yaml
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "0"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "compute,graphics,utility,video,display"
          ports:
            - name: http-api
              containerPort: 8080
          volumeMounts:
            - name: pipeline-config
              mountPath: /home/paddleocr/pipeline_config_fastdeploy.yaml
              subPath: pipeline_config_fastdeploy.yaml
              readOnly: true
          resources:
            requests:
              cpu: {{ .Values.engine.resources.requests.cpu | quote }}
              memory: {{ .Values.engine.resources.requests.memory | quote }}
              nvidia.com/gpumem: {{ $gpuMemMi | quote }}
            limits:
              cpu: {{ .Values.engine.resources.limits.cpu | quote }}
              memory: {{ .Values.engine.resources.limits.memory | quote }}
        - name: nginx
          image: "{{ .Values.frontend.image }}"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: NGINX_HOST
              value: "0.0.0.0"
            - name: NGINX_PORT
              value: "80"
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 10
            timeoutSeconds: 5
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 30
            successThreshold: 1
            failureThreshold: 3
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: nginx.conf
              readOnly: true
          resources:
            {{- toYaml .Values.frontend.resources | nindent 12 }}